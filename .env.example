# GitHub token used for the search API and for filing issues.
# Needs read access for search and write access (repo scope) on the issue target repo.
# The Reddit source is anonymous public RSS and needs no credentials by design.
SCOUT_GITHUB_TOKEN=
